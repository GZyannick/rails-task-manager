<h1>My tasks</h1>
<ul>
  <% @tasks.each do |task| %>
  <li> <%= task.title %> || complet: <%= task.completed %> <%= link_to 'Details', task_path(task) %></li>
  <% end %>
</ul>
<%= link_to 'New task', tasks_new_path %>