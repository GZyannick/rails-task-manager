<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<p>complet?: <%= @task.completed %></p>
<%= link_to 'All', tasks_path %>
<%= link_to 'edit', edit_task_path %>
<%= link_to 'delete', 
  task_path,
  method: :destroy,
  data: { confirm: 'Are you sure?' }
%>